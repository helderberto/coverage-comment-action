{
  "name": "coverage-comment-action",
  "version": "1.0.0",
  "description": "GitHub Action to post coverage reports as PR comments",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github-action",
    "coverage",
    "pull-request",
    "comment",
    "testing"
  ],
  "author": "Helder Burato Berto <helder.burato@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
